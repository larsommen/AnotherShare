After changes:

git add -A

git commit -m (and describe what changes you have made)

git push


To get changes:

git pull


Remember to be in the directory when you write the above commands


To add new repository:

git clone http-address_from_github

e.g.

git clone https://github.com/larsommen/AnotherShare
